# Debian build artifacts
/build/
# tools/expose-debs.sh copies packages here for the shared lintian glob, so a
# local CI reproduction leaves them in the checkout root.
/*.deb
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/halos-core-containers/
debian/*.substvars
debian/*.debhelper

# Local git worktrees for parallel work
.worktrees/

# ca-download endpoint test scratch dirs (cleaned by the test's trap)
.catest.*
